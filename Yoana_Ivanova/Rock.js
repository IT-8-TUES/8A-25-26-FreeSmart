// Executes code once the DOM is fully loaded
document.addEventListener('DOMContentLoaded', () => {
    console.log("Rock genre section initialized successfully.");

    // You can use this block later to dynamically generate your artist items
    /* const container = document.getElementById('artists-container');
    const artists = ['Led Zeppelin', 'The Rolling Stones', 'Pink Floyd'];
    
    artists.forEach(artist => {
        const card = document.createElement('div');
        card.innerText = artist;
        container.appendChild(card);
    });
    */
});